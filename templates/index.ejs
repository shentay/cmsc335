<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="/javascript/authenticate.js"></script>
</head>
<body>
    <div class="authenticate">
        <h1>REGISTER</h1>
        <hr>
        <form action=<%- localhost %> method="POST" onsubmit="return verifyPassword()">
            <div id="error-message">
                <%- message %>
            </div>
            <div>
                <label for="email">Email</label>
                <input type="email" name="email" id="email" required>
            </div>
            <div>
                <label for="password">Password</label>
                <input type="password" name="password" id="password">
            </div>
            <div>
                <label for="password-rpt">Repeat Password</label>
                <input type="password" name="password-rpt" id="password-rpt">
            </div>
            <button type="submit" class="submitbtn">Register</button>
        </form>
    </div>
    <div class="authenticate-options">
        <p>If you already have an account, you can <a href="/login">Sign in</a>.</p>
    </div>
</body>